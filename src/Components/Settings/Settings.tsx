import { FC } from 'react';
import classes from './Settings.module.css';

const Settings: FC = () => {
    return (
        <div>
            Settings
        </div>
    );
}

export default Settings;